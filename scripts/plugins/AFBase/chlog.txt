.player_setmaxspeed переменовался в .player_maxspeed
частота обновления для копирования и хватания теперь выше, для более плавного опыта
копирование\хватание теперь показует границы.
добавлена поддержка команд из модификаторов инвентаря для player_keyvalue (типо m_iEffect- и m_flEffect-) 
добавлена поддержка m_iDeaths для player_keyvalue
r#a-b изменился в формат r#a#b что позволит изпользовать цыфры ниже нуля (к примеру. r#-100#100)
новая команда: .ent_bbox
новая команда: .ent_bboxname
новая команда: .ent_grid
исправлена эксплуатация в котором ent_* команды могли обойти доступ С для rcon - любые модификации для trigger_setcvar теперь требуют доступ С
новая команда: .afb_whatsnew 
новая команда: .fun_fog
спрей баны теперь обрабатываються через хук заместь изменения времени спрея
admin_kick теперь не должен показывать откладочный результат (упс) 
admin_ban теперь не показует ошибку когда ты изпользуешь стандартные значения
admin_ban теперь не позволит бан без пустого строка которая полностью ломает привратник
исправлен внутренний баг: консольные комманды и регистрации теперь внутренно всегда со строчной буквы, чтобы скриптеры с дополнениями не марочились с чуствительность букв в registercommand
исправлен баг в котором регистрация команд непозволяла регестрировать со строчной буквы и делал бы конфликты с консолью
.player_giveammo/say !giveammo теперь имееют способность на наполнение всех патрон на каждом пушке цели - Теперь делает по стандрату, но можно отрубить изпользуя дополнитильные параметры
.player_disarm теперь может убирать одно оружие по выбору
исправлена проблема с делением на ноль с .ent_rotate
.ent_mover теперь уже не глюканутый виюмодель, он теперь работает как скрипт на оружия для визуальных целей и дейсвует как ентмодовский entmover
новая команда: .ent_show
.player_giveall теперь поддержует популярные плагины на оружия
.afb_menu теперь показует @target(ты) в красном чтобы убрать путанину с игроками похожими никами
.afb_menu теперь обращяет цели внутренно со SteamId чем с ихними никами